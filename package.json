{
  "name": "@12urenloop/manual-count-2",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "pn -r --stream dev",
    "build": "pn -r --stream build && cd ../.. && pn -r --stream start",
    "pre-commit-checks": "pretty-quick --staged --ignore-path .prettierignore"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.5.3",
    "pretty-quick": "^4.1.1"
  }
}
